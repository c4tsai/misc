#ifndef __AREA_H__
#define __AREA_H__

#include <string>
#include <vector>

#include "cells.h"

std::vector<Cell*> parse(std::string fname, Board& b)

void cell_distance(Cell& cell, const Cell& target);

void count_distances(Board& b);

std::string count_area


#endif